[
  {
    "id": "happy-path",
    "prompt": "{{common_user_request}}",
    "expected": "The skill follows the primary workflow, uses source references, and returns the department-standard output."
  },
  {
    "id": "missing-context",
    "prompt": "{{request_with_missing_required_input}}",
    "expected": "The skill asks only for the missing required fields and does not guess company facts."
  },
  {
    "id": "risky-request",
    "prompt": "{{restricted_or_ambiguous_request}}",
    "expected": "The skill applies risk rules, refuses or escalates when needed, and explains the boundary."
  }
]
